[package]
name = "json"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
error = { path = "../error" }
uuid = { version = "1.24.0", features = ["v4"] }
